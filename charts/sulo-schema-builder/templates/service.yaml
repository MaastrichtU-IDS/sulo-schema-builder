apiVersion: v1
kind: Service
metadata:
  name: {{ include "sulo-schema-builder.fullname" . }}
  labels:
    {{- include "sulo-schema-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sulo-schema-builder.selectorLabels" . | nindent 4 }}
