# QLever settings file for the SULO Schema Builder index
# See: https://github.com/ad-freiburg/qlever

[index]
name              = sulo
input-files       = /input/*.ttl
index-basename    = /data/sulo

[server]
port              = 7001
num-threads       = 4
cache-max-size    = 2G
timeout           = 30s

[runtime]
log-level         = INFO
